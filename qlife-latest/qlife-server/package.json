{
  "name": "qlife-server",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "dist/src/main.js",
  "scripts": {
    "start": "node dist/src/main.js",
    "start:dev": "ts-node --transpile-only src/main.ts",
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "format": "prettier --write .",
    "test": "NODE_ENV=test jest",
    "test:watch": "NODE_ENV=test jest --watch",
    "test:e2e": "NODE_ENV=test jest --config test/jest-e2e.json",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "migrate:local": "sh -c 'export DATABASE_URL=$(grep -E \"^DATABASE_URL=\" ./.env | head -1 | cut -d= -f2-); prisma migrate dev; CODE=$?; unset DATABASE_URL; exit $CODE'",
    "migrate:prod": "sh -c 'export DATABASE_URL=$(grep -E \"^DATABASE_URL=\" ./.env.production | head -1 | cut -d= -f2-); prisma migrate deploy; CODE=$?; unset DATABASE_URL; exit $CODE'",
    "seed:prod": "sh -c 'export DATABASE_URL=$(grep -E \"^DATABASE_URL=\" ./.env.production | head -1 | cut -d= -f2-); prisma db seed; CODE=$?; unset DATABASE_URL; exit $CODE'",
    "seed:extract:legacy": "ts-node --transpile-only tools/extract-legacy-seed.ts",
    "seed": "prisma db seed",
    "db:reset:local": "sh -c 'export DATABASE_URL=$(grep -E \"^DATABASE_URL=\" ./.env | head -1 | cut -d= -f2-); prisma migrate reset --force; CODE=$?; unset DATABASE_URL; exit $CODE'",
    "db:reset:prod": "sh -c 'export DATABASE_URL=$(grep -E \"^DATABASE_URL=\" ./.env.production | head -1 | cut -d= -f2-); prisma migrate reset --force; CODE=$?; unset DATABASE_URL; exit $CODE'",
    "admin:verifications": "ts-node --transpile-only tools/admin-verifications.ts",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down -v"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@nestjs/common": "^11.1.27",
    "@nestjs/config": "^4.0.4",
    "@nestjs/core": "^11.1.27",
    "@nestjs/platform-express": "^11.1.27",
    "@nestjs/platform-socket.io": "^11.1.27",
    "@nestjs/testing": "^11.1.27",
    "@nestjs/throttler": "^6.5.0",
    "@nestjs/websockets": "^11.1.27",
    "@prisma/client": "^6.19.3",
    "@workos-inc/node": "^10.7.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "firebase-admin": "^13.2.0",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "jwk-to-pem": "^2.0.7",
    "nodemailer": "^9.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/jwk-to-pem": "^2.0.3",
    "@types/node": "^25.9.3",
    "@types/nodemailer": "^8.0.1",
    "@types/supertest": "^7.2.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.39.4",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "jest": "^30.4.2",
    "prettier": "^3.8.4",
    "prisma": "^6.19.3",
    "rimraf": "^6.1.3",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.11",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "prisma": {
    "seed": "ts-node --transpile-only prisma/seed.ts"
  }
}
