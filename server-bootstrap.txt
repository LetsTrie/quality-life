sudo apt update
git config --global user.name "letstrie"
git config --global user.email "sakibkhan111296@gmail.com"

sudo apt update && sudo apt install nginx certbot python3-certbot-nginx && sudo ufw status && sudo ufw enable && sudo ufw allow 80 && sudo ufw allow 443 && sudo ufw allow 22 && sudo ufw allow 'Nginx HTTP' && sudo ufw status
sudo systemctl start nginx
sudo systemctl enable nginx
systemctl status nginx

sudo nano /etc/nginx/sites-available/default
>>> After updating the nginx file <<<
sudo systemctl daemon-reload
sudo systemctl reload nginx

npm install

npm i pm2 -g 
pm2 start index.js --name "api_prod" -i 1

********************
Node Installation
********************

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install v20.11.0
node -e "console.log('Running Node.js ' + process.version)"

********************
NANO Documentation
********************

* Press Ctrl + K to cut the line
* Press Ctrl + U to undo